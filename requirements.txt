# ── Web framework ────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Pydantic settings ────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── CV / ML core ─────────────────────────────────────────────
opencv-python>=4.9.0
mediapipe==0.10.11
scikit-learn>=1.4.0
numpy>=1.24.0
joblib>=1.4.0

# ── Data / training ─────────────────────────────────────────
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Grammar correction ───────────────────────────────────────
spacy>=3.7.0
# After install run: python -m spacy download en_core_web_sm
language_tool_python>=2.7.1    # optional — needs Java installed

# ── TTS fallback (if browser TTS unavailable) ────────────────
pyttsx3>=2.90

# ── Optional: GPT grammar (uncomment + set OPENAI_API_KEY) ──
# openai>=1.30.0
