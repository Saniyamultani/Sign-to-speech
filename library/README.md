# Sign Video Library

Place `.mp4` sign language videos here — one file per word.

Name each file exactly after the sign word it shows:

```
library/
├── hello.mp4
├── thankyou.mp4
├── yes.mp4
├── no.mp4
└── water.mp4
```

Supported formats: `.mp4`, `.avi`, `.mov`, `.webm`

The Text → Gesture page will automatically list all videos found here.
